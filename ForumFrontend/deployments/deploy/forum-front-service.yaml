apiVersion: v1
kind: Service
metadata:
  name: forum-front-service
  namespace: forum
spec:
  type: NodePort
  selector:
    app: forum-front
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
