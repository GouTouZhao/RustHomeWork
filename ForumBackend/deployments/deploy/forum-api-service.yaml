apiVersion: v1
kind: Service
metadata:
  name: forum-api-service
spec:
  selector:
    app: forum-api
  ports:
    - name: http-10002
      protocol: TCP
      port: 10002
      targetPort: 8080
      nodePort: 30002
  type: NodePort
